name: zxz
help: a CLI to interact with <https://0x0.st/>
version: 0.0.2

dependencies:
  - curl

args:
  - name: file
    required: true
    help: file to upload
    validate: file_exists

examples:
  - zxz <filename>
